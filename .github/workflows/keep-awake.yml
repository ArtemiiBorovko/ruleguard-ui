name: Keep Render Awake
on:
  schedule:
    - cron: '*/10 4-18 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -s -o /dev/null -w "%{http_code}\n" https://ruleguard-backend.onrender.com/
